<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.userddssilva.contactsapp">

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhonebookByMe"
        tools:ignore="AllowBackup">
        <activity
            android:name="br.com.userddssilva.contactsapp.view.EditContact"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name="br.com.userddssilva.contactsapp.view.CreateContact"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name="br.com.userddssilva.contactsapp.view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>